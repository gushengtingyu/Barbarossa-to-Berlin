# Source assets

This directory keeps editable or provenance-only inputs outside Rally's runtime asset tree.

- `card-sheets/`: original Chinese card sheets consumed by `tools/slice_cards.py`.
- `vassal/`: review-only VASSAL module sources and unused artwork.

Runtime assets remain in the paths declared by `rally-assets.json`.
